[pomodoro]
# Defaults in minutes. Saved duration edits take priority.
# Each value must be 1–59; focus + either rest must not exceed 60.
# Invalid values use defaults; an invalid total resets all three defaults.
focus = 25
rest = 5
long-rest = 15

[notifications]
# Positive multiples of 5 minutes; invalid values use 5.
popup_time = 5

# Paths are relative to this file.
green_notification = "green_notification.mp3"
yellow_notification = "yellow_notification.mp3"
red_notification = "red_notification.mp3"
alarm_notification = "alarm_notification.mp3"
